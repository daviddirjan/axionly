# Romanian is now the default locale and lives at the root; English moved to /en.
# Netlify / Cloudflare Pages syntax — adjust if the site is hosted elsewhere.

# Old Romanian URLs (/ro/*) → the root
/ro/evaluare-rapida   /evaluare-rapida   301
/ro                   /                  301
/ro/*                 /:splat            301

# The only English page whose slug has no Romanian twin at the root
/quick-check          /en/quick-check    301
